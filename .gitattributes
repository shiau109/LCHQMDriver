# Auto detect text files and perform LF normalization
* text=auto

# Shell launchers must stay LF or they break on macOS/Linux (CR in shebang)
*.command text eol=lf
*.sh      text eol=lf

# Windows batch launchers use CRLF
*.bat     text eol=crlf
