"""Run qubit_ramsey directly (AUTO-GENERATED by _sync.py — regenerate, never edit).

Two pi/2 pulses separated by a swept idle time with an artificial drive detuning; fits the decaying fringe to correct drive_freq and report T2*.

    python scripts/experiments/qubit_ramsey.py --qubits q0 q1 --tag mytag
    python scripts/experiments/qubit_ramsey.py --help        # full parameter list
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from _cli import run_experiment_cli

if __name__ == "__main__":
    sys.exit(run_experiment_cli("qubit_ramsey", doc=__doc__))
