"""Run qubit_echo directly (AUTO-GENERATED by _sync.py — regenerate, never edit).

Hahn echo (X90 - tau/2 - X - tau/2 - X90) over a swept total idle time; fits the exponential envelope and proposes t2_echo_s as a physical parameter (sample physics, no instrument knob).

    python scripts/experiments/qubit_echo.py --qubits q0 q1 --tag mytag
    python scripts/experiments/qubit_echo.py --help        # full parameter list
    (equivalent: scqo run qubit_echo ... — works from any directory)
"""

import sys

from scqo.cli import run_experiment_cli

if __name__ == "__main__":
    sys.exit(run_experiment_cli("qubit_echo", doc=__doc__))
