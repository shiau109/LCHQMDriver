"""Run resonator_spectroscopy_flux directly (AUTO-GENERATED by _sync.py — regenerate, never edit).

2D resonator spectroscopy vs flux bias: tracks the dip at every flux and fits the dispersive model; reports flux sweet spot, flux period, bare f_r0 and coupling g (no device writeback).

    python scripts/experiments/resonator_spectroscopy_flux.py --qubits q0 q1 --tag mytag
    python scripts/experiments/resonator_spectroscopy_flux.py --help        # full parameter list
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from _cli import run_experiment_cli

if __name__ == "__main__":
    sys.exit(run_experiment_cli("resonator_spectroscopy_flux", doc=__doc__))
