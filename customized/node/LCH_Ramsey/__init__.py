"""Qualibrate-side code for the LCH_Ramsey node: scqat analysis adapter and
state-update policy. The node's Parameters are the vendored official ones
(`calibration_utils.ramsey.Parameters`); the acquisition probe lives in
`customized/probes/ramsey/`."""
